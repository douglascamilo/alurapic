import { PhotoList.Resolver } from './photo-list.resolver';

describe('PhotoList.Resolver', () => {
  it('should create an instance', () => {
    expect(new PhotoList.Resolver()).toBeTruthy();
  });
});
